## 데이터베이스 정규화
# 1차 정규화
- 로우마다 컬럼의 값이 1개씩만 있어야 한다.
- 컬럼이 원자값(atomic value)을 갖는다.

# 2차 정규화
- 테이블의 모든 컬럼이 완전 함수적 종속을 만족해야한다.
- 기본키중에 특정 컬럼에만 종속된 컬럼(부분적 종속)이 없어야한다.

# 3차 정규화
- 기본키를 제외한 속성들 간의 이행적 함수 종속이 없는 것
- 기본키 이외의 다른 컬럼이 그 외 다른 컬럼을 결정할 수 없다.

// 해결법 : 테이블 분리!!!!!